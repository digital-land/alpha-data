{
    "fields": [
         {
            "name": "developer-contribution-purpose",
            "title": "Developer contribution purpose",
            "description": "A unique ID for the contribution purpose",
            "type": "string",
            "constraints": {
                "required": true,
                "pattern": "^([a-z][a-z0-9]*)(-[a-z0-9]+)*$"
            }
        },
        {
           "name": "name",
           "title": "Name",
           "description": "Name for the contribution purpose",
           "type": "string",
           "constraints": {
               "required": true
           }
       },
        {
            "name": "entry-date",
            "title": "Entry date",
            "description": "The date that the record was created",
            "type": "date",
            "constraints": {
                "required": true
            }
        },
        {
            "name": "start-date",
            "title": "Start date",
            "description": "The date on which the entry comes into effect",
            "type": "date",
            "constraints": {
                "required": false
            }
        },
         {
            "name": "end-date",
            "title": "End date",
            "description": "The date on which the entry is not longer in effect",
            "type": "date",
            "constraints": {
                "required": false
            }
        }
    ]
}
